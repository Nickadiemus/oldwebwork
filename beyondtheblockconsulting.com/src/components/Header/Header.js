import React from 'react';
import Link from 'gatsby-link';

const Header = () => (
    <div style = {{
      margin: '0px',
      padding: '0px',
    }} className = "container-fluid">
    </div>
);

export default Header;
